[**signal-piu**](../../../README.md)

***

[signal-piu](../../../README.md) / [runtime/flow](../README.md) / Navigator

# Function: Navigator()

> **Navigator**(`props`): `any`

Defined in: src/tsx/globals.d.ts:210

Screen stack — only the TOP screen is built, so the arena is O(1) at any depth. `root(nav)` builds the first screen; `nav.push/pop` navigate.

## Parameters

### props

[`NavigatorProps`](../interfaces/NavigatorProps.md)

## Returns

`any`
