[**pebble-signals**](../../README.md)

***

[pebble-signals](../../README.md) / [signals](../README.md) / withBoundary

# Function: withBoundary()

> **withBoundary**\<`T`\>(`handler`, `fn`): `T`

Defined in: [signals.ts:793](https://github.com/emindeniz99/pebble-signals/blob/main/src/embeddedjs/runtime/signals.ts#L793)

Run `build` with `handler` as the active ErrorBoundary (internal — see flow's ErrorBoundary).

## Type Parameters

### T

`T`

## Parameters

### handler

((`e`) => `void`) \| `null`

### fn

() => `T`

## Returns

`T`
