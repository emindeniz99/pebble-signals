// Second convention screen — proves the whole folder ships, not just the
// first file the entry happens to reach.
export default () => (
	<Column>
		<Label string="screens/beta.tsx" />
		<Label string="loaded on 2nd select" />
	</Column>
);
