// Auto-shipped by the screens/ folder convention — no importNow literal
// for this file exists anywhere; the entry computes the name at runtime.
export default () => (
	<Column>
		<Label string="screens/alpha.tsx" />
		<Label string="auto-shipped, lazy" />
	</Column>
);
