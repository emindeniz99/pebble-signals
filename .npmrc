# pnpm 11 reads its settings from pnpm-workspace.yaml (supply-chain
# minimumReleaseAge etc. live THERE) — this file stays only for npm-visible
# registry behavior if anyone runs npm here by mistake.
